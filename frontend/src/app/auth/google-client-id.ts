// Client ID do OAuth do Google (não é segredo — pode ficar público/commitado).
// Crie um em https://console.cloud.google.com/apis/credentials
// ("Create Credentials" → "OAuth client ID" → tipo "Web application")
// e adicione http://localhost:4200 em "Authorized JavaScript origins".
export const GOOGLE_CLIENT_ID = 'SUBSTITUA_PELO_SEU_CLIENT_ID.apps.googleusercontent.com';
